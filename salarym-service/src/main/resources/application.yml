
spring:
  application:
    name: salarym-service
  config:
    import: "optional:configserver:http://localhost:8088"
  cloud:
    compatibility-verifier:
      enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

grpc:
  server:
    port: 9091

server:
  port: 8087

